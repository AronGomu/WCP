export class Cost {

    unc; // int, uncolor cost
    red; // int
    blue; // int
    green; // int

    constructor(unc, red, blue, green) {
        this.unc = unc;
        this.red = red;
        this.blue = blue;
        this.green = green;
    }
}